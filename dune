(executable
	 (name main)
   (modules main parser lexer ast env eval interp)
	 (libraries graphics unix)) 
(ocamllex (modules lexer))
(menhir
  (modules parser)
  (infer true)
  (flags --explain --dump))
