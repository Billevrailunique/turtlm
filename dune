(executable
	 (name main)
   (modules main parser lexer ast env eval interp parserMessages)
	 (libraries graphics unix menhirLib)) 
(ocamllex (modules lexer))
(menhir
  (modules parser)
  (infer true)
  (flags --explain --dump --table))
